{
  "name": "@secureid/face-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app:app --reload --port 8001",
    "start": "uvicorn app:app --port 8001",
    "test": "python -m pytest tests/ -v"
  }
}
